// Vendored UMD bundles inlined into the offline interactive-HTML export
// (see export/toHtml.ts). d3 sets `window.d3`; Plot then sets `window.Plot`.
// Refresh by copying from node_modules when d3 / @observablehq/plot are upgraded.
